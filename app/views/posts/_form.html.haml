= simple_form_for(@post, html: { class: 'form-horizontal' }) do |f|
  .container.top-50
    .form-group
      = f.input :title
      = f.input :content, :input_html => {:rows => 10}
      = f.input :placename
    = f.button :submit